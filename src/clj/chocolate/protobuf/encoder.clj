(ns chocolate.protobuf.encoder
  (:require [protobuf.core :as protobuf]))
  ;(:import java.util.Date))


;(defmethod encode-content "Person" [msg]
;  (assoc msg :content (-> (protobuf/create Example$Person (content msg))
;                          (protobuf/->bytes))))

